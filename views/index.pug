extends layout

block content
  h1= title
  p Welcome to #{title}

  // indentation is important!
  ul 
    each message in messages
      li= message.user + ": " +  message.text + "- " + message.added.toDateString()
